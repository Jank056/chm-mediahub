# PostgreSQL
POSTGRES_USER=mediahub
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
POSTGRES_DB=mediahub

# JWT Secret (generate with: openssl rand -hex 64)
JWT_SECRET=CHANGE_THIS_GENERATE_NEW_SECRET

# Webhook API key for ops-console sync (generate with: openssl rand -hex 32)
WEBHOOK_API_KEY=CHANGE_THIS_MUST_MATCH_OPS_CONSOLE

# Chatbot API (your existing chatbot deployment)
CHATBOT_API_URL=http://13.220.20.135:8000

# CORS - your production frontend URL
CORS_ORIGINS=["https://mediahub.yourdomain.com"]

# Frontend API URL (backend URL as seen from browser)
NEXT_PUBLIC_API_URL=https://api.mediahub.yourdomain.com
